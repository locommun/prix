<%= simple_form_for(@announcement) do |f| %>
 
  <%= f.error_notification %>
  
	<div class="form-inputs">
		<%= f.input :name , :label => "Titel"%>
		<%= f.input :description, :input_html => { :rows => 5 } , :label => "Beschreibung"%>
		<%= f.input :datetime_type, :as => :radio_buttons,:label => "Termin", :collection => {
			"Kein Termin festlegen" => "no_date" , 
			"Termin-Umfrage schalten" => "date_suggest" , 
			raw("Termin festlegen: " + select_datetime(@announcement.datetime , :datetime_separator => ' Uhrzeit: ')) => "date_fixed"} %>
		<span class="control-label">Module aktivieren</span>
		<%= f.input :uj , :label => false,:inline_label => 'Mitmachliste'%>
		<%= f.input :bt , :label => false,:inline_label => 'Mitbringliste'%>
		<span class="control-label">Standort der Aktivität</span><br/> <i>(wird nur angezeigt, wenn angegeben)</i>
		<% if request.location %>
		  <%= gmaps({ 
			"map_options" => { "container_class" => "map_container map_container_form" , "center_longitude" => request.location.longitude,"center_latitude" => request.location.latitude, "zoom" => 12, "auto_adjust" => true},
			"markers" => {"data" => @json}
			}) %> 
		<%else%>
			 <%= gmaps({ 
			"map_options" => { "container_class" => "map_container map_container_form" , "zoom" => 1, "auto_adjust" => true , "auto_zoom" => false },
			"markers" => {"data" => @json}
			}) %> 
		<%end%>
		  <% content_for :scripts do %>
		    <script type="text/javascript" charset="utf-8">
		        var markersArray = [];
		        // On click, clear markers, place a new one, update coordinates in the form
		        Gmaps.map.callback = function() {
		            google.maps.event.addListener(Gmaps.map.serviceObject, 'click', function(event) {
		              clearOverlays();
		              placeMarker(event.latLng);
		              updateFormLocation(event.latLng);
		            });
		        };
		        // Update form attributes with given coordinates
		        function updateFormLocation(latLng) {
		            $('#billboard_latitude').val(latLng.lat());
		            $('#billboard_longitude').val(latLng.lng());
		            
		        }
		        // Add a marker with an open infowindow
		        function placeMarker(latLng) {
		            var marker = new google.maps.Marker({
		                position: latLng, 
		                map: Gmaps.map.serviceObject,
		                draggable: true
		            });
		            markersArray.push(marker);
		            //Gmaps.map.replaceMarkers(markersArray)
		            // Set and open infowindow
		            var infowindow = new google.maps.InfoWindow({
		               
		            });
		            infowindow.open(Gmaps.map.serviceObject,marker);
		            // Listen to drag & drop
		            google.maps.event.addListener(marker, 'dragend', function() {
		                updateFormLocation(this.getPosition());
		            });
		        }
		        // Removes the overlays from the map
		        function clearOverlays() {
		          if (markersArray) {
		            for (var i = 0; i < markersArray.length; i++ ) {
		              markersArray[i].setMap(null);
		            }
		          }
		          markersArray.length = 0;
		        }
		    </script>
		  <% end %></br>
		  
			<%= f.input :latitude ,:as => :hidden, :input_html => {:id => "billboard_latitude", :readonly => true}%>
			<%= f.input :longitude,:as => :hidden, :input_html => {:id => "billboard_longitude", :readonly => true}%>
		
		<%= f.input :billboard_id , :as => :hidden, :input_html => {:value => @billboard.id} %>
		<br />
	</div>
  
  	<%= f.submit "Aushang speichern", :class => "btn btn-primary" %>
  	
<% end %>
